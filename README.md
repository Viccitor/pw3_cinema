# pw3-ingresso

## Integrantes
- Victor de Melo
- Renato Luiz


CREATE DATABASE pw3_ingressos;

USE pw3_ingressos;

CREATE TABLE TBL_FILME 
(ID_FILME BIGINT PRIMARY KEY IDENTITY,TX_NOME VARCHAR(50) NOT NULL,
NR_DURACAO INT NOT NULL,TP_CATEGORIA VARCHAR(17) NOT NULL,
TP_CLASSIFICACAO VARCHAR(5),
CHK_EM_CARTAZ VARCHAR(1),
NR_ANO INT,
TX_CAPA VARCHAR(300),
TX_DIRETOR VARCHAR(300),
TX_ELENCO VARCHAR(300),
TX_DESCRICAO VARCHAR(500),
NR_AVALIACAO FLOAT);

SELECT * FROM TBL_FILME ORDER BY NR_AVALIACAO DESC

INSERT INTO TBL_FILME
(TX_NOME, NR_DURACAO, TP_CATEGORIA, TP_CLASSIFICACAO, CHK_EM_CARTAZ, NR_ANO, TX_CAPA, TX_DIRETOR, TX_ELENCO, TX_DESCRICAO, NR_AVALIACAO)
VALUES
('Superman Legacy', 120, 'ACAO', 'A14', 'S', 2015, 'superman.jpg', 'James Gunn', 'David Corenswet, Rachel Brosnahan, Nicholas Hoult', 'O universo DC imaginado do zero com uma mistura singular de ação épica, humor e emoção', 7.6),CREATE TABLE TBL_SESSAO(    ID_SESSAO INT NOT NULL PRIMARY KEY IDENTITY,    ID_FILME BIGINT NOT NULL,    TX_DATA DATETIME,    TX_HORARIO TIME,    NR_PRECO FLOAT NOT NULL,    TX_SALA VARCHAR(50) NOT NULL,    CONSTRAINT FIL_FK_SES FOREIGN KEY(ID_FILME) REFERENCES TBL_FILME);INSERT INTO TBL_SESSAO VALUES(6, '2025-07-10 11:00:00', '11:00:00', 19.99, 'Sala 1')

